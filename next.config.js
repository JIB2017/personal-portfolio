/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn.sanity.io", "images.crunchbase.com", "cdn.worldvectorlogo.com"],
    }
}

module.exports = nextConfig
